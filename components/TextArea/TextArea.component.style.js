import { StyleSheet } from "react-native";

export default StyleSheet.create ({
    textArea: {
        borderColor : 'gray',
        borderWidth : 2,
        alignItems : "center",
        justifyContent : "center",
        alignContent : "center",
    }
})