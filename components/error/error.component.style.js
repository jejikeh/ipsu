import { StyleSheet } from "react-native";

export default StyleSheet.create ({
    container : {
        textAlign : "center",
        justifyContent : "center",
        alignContent : "center",
    }
});